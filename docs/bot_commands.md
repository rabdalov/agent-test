# Команды Telegram-бота

Бот принимает аудио- и видеофайлы (или HTTP-ссылки на них) и автоматически создаёт MP4-видео с синхронизированными субтитрами в стиле караоке. Управление процессом обработки осуществляется через команды ниже.

## Команды для вставки в BotFather

start - Приветственное сообщение. Объясняет, что бот делает и как его использовать.
search - Поиск трека в локальном хранилище или на Яндекс-музыке
change - Изменение типа сегментов в разметке громкости (после /step_chorus)
continue - Продолжает обработку последнего трека с последней ошибки или остановки
step_download - Скачивание исходного аудио- или видеофайла
step_language - Запрос выбора языка у пользователя
step_lyrics - Поиск и получение полного текста песни
step_separate - Разделение голоса и музыки
step_transcribe - Транскрибация вокальной дорожки
step_generate_lyrics - Генерация текста песни из транскрипции
step_chorus - Определение припевов и формирование разметки громкости вокала
step_mix - Обработка вокала: применение разметки громкости и создание бэк-вокального микса
step_correct - Уточнение транскрипции через LLM
step_align - Cопоставление текста песни с таймкодами слов.
step_ass - Генерация файла субтитров .ass
step_render - Сборка MP4-видео с субтитрами
step_send - Отправка готового видео пользователю
step_visualize - Отправка PNG-визуализации таймлайна сегментов трека

## Команда /change — изменение типа сегментов

После выполнения шага `DETECT_CHORUS` вы можете вручную скорректировать типы сегментов (припев, куплет, инструментал) и автоматически пересчитать аудиомикс.

### Формат команды

```
/change <диапазон>
```

Поддерживаемые форматы диапазона:
- **Одиночный сегмент**: `/change 5`
- **Перечисление**: `/change 1,3,5`
- **Диапазон**: `/change 5-10`
- **Смешанный формат**: `/change 1,3,5-7,9`

### Сценарий использования

1. Выполните шаг `/step_chorus` для определения припевов
2. Посмотрите визуализацию сегментов через `/step_visualize` (если нужно)
3. Используйте `/change`, чтобы скорректировать типы сегментов:
   ```
   /change 3-5
   ```
4. Бот покажет inline-кнопки выбора типа: 🎤 Припев, 📝 Куплет, 🎹 Инструментал
5. После выбора типа бот обновит `volume_segments.json` с новыми значениями громкости
6. Нажмите кнопку «🔄 Пересчитать» для запуска пайплайна с шага `MIX_AUDIO`

### Примеры

```
/change 5          # Изменить тип сегмента #5
/change 3-5        # Изменить сегменты #3, #4, #5
/change 1,3,5-7    # Изменить сегменты #1, #3, #5, #6, #7
```

### Типы сегментов и громкость

| Тип | Описание | Громкость вокала |
|-----|----------|------------------|
| `chorus` | Припев | `CHORUS_BACKVOCAL_VOLUME` (по умолчанию 0.4) |
| `verse` | Куплет | `AUDIO_MIX_VOICE_VOLUME` (по умолчанию 0.2) |
| `instrumental` | Инструментальный отрывок | `AUDIO_MIX_VOICE_VOLUME` (по умолчанию 0.2) |

## Сценарии использования

### 1. Базовый сценарий

Отправьте боту аудио- или видеофайл в одном из поддерживаемых форматов (`MP3`, `FLAC`, `AVI`, `MP4`, `MKV`, длительность > 1 минуты) или прямую HTTP-ссылку на файл. Также поддерживаются ссылки с YouTube и Яндекс.Музыки.

Бот автоматически запустит полный пайпайн обработки:

```
DOWNLOAD → ASK_LANGUAGE → GET_LYRICS → SEPARATE → TRANSCRIBE → GENERATE_LYRICS → DETECT_CHORUS → CORRECT_TRANSCRIPT → ALIGN → MIX_AUDIO → GENERATE_ASS → RENDER_VIDEO → SEND_VIDEO
```

> **Примечание:** Шаг `GENERATE_LYRICS` выполняется только если текст песни не найден внешними сервисами и пользователь выбрал вариант "Транскрипция". При этом генерируется текст из сегментов транскрипции для последующего подтверждения.

По завершении каждого шага бот отправит статусное сообщение. После успешного прохождения всех шагов вы получите готовый MP4-файл с субтитрами в стиле караоке.

### 2. Продолжение после ошибки

Если один из шагов завершился с ошибкой:

1. Изучите сообщение об ошибке, которое прислал бот.
2. Устраните причину сбоя (например, проблему с внешним сервисом или входными данными).
3. Используйте `/continue` для повтора неудавшегося шага или одну из команд `/step_*` для запуска пайплайна с любого конкретного шага.

## Шаги пайплайна

| Шаг | Команда запуска | Описание |
|---|---|---|
| `DOWNLOAD` | *(запускается автоматически при получении файла/ссылки)* | Скачивание исходного аудио- или видеофайла из любого источника: Telegram-файл, HTTP URL, Яндекс.Музыка, YouTube, локальный файл. |
| `ASK_LANGUAGE` | `/step_language` | Запрос выбора языка песни у пользователя (Русский/English). Пайплайн приостанавливается до выбора. |
| `GET_LYRICS` | `/step_lyrics` | Получение текста песни: автоматический поиск через сервисы (Genius, Lyrica, lyrics-lib) или ручной ввод от пользователя. |
| `SEPARATE` | `/step_separate` | Разделение аудиодорожки на вокал и музыкальный фон с помощью **demucs**. |
| `TRANSCRIBE` | `/step_transcribe` | Транскрибация вокальной дорожки — распознавание слов с таймкодами через **speeches.ai**. |
| `GENERATE_LYRICS` | `/step_generate_lyrics` | Генерация текста песни из сегментов транскрипции (fallback при отсутствии текста из внешних сервисов). Выполняется только при `use_transcription_as_lyrics=true`. |
| `DETECT_CHORUS` | `/step_chorus` | Определение временных отрезков припевов через **msaf**/**librosa**/**hybrid**, формирование разметки громкости вокала и сохранение в `{stem}_volume_segments.json`. Пропускается при `MIX_AUDIO_ENABLED=false`. |
| `CORRECT_TRANSCRIPT` | `/step_correct` | Уточнение транскрипции с использованием LLM на основе текста песни (повышает качество выравнивания). |
| `ALIGN` | `/step_align` | Сопоставление (выравнивание) эталонного текста песни с таймкодами транскрибированных слов. |
| `MIX_AUDIO` | `/step_mix` | Применение разметки громкости (из `volume_segments_file`) к вокальной дорожке и создание бэк-вокального микса `instrumental + processed_vocal`. Пропускается при `MIX_AUDIO_ENABLED=false`. |
| `GENERATE_ASS` | `/step_ass` | Генерация файла субтитров в формате `.ass` с покадровой подсветкой слов. |
| `RENDER_VIDEO` | `/step_render` | Финальная сборка MP4-видео: наложение субтитров на исходное видео (или заставку) через **ffmpeg**. Создаётся видео с тремя (или четырьмя) аудиодорожками: Instrumental, Original, Instrumental+Voice, Instrumental+BackVocal (если выполнен шаг `MIX_AUDIO`). |
| `SEND_VIDEO` | `/step_send` | Отправка готового MP4-файла пользователю или формирование ссылки на скачивание. |
| *(вспомогательная)* | `/step_visualize` | Отправка PNG-визуализации таймлайна сегментов трека (если `TRACK_VISUALIZATION_ENABLED=true` и файл существует). |
